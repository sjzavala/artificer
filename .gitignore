# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# local data store (never commit demo/deal data)
/data/
/public/uploads/

# env files — never commit secrets
.env
.env.local
.env.*.local
.env.production
.env.development

# eval output
evals/results/

# vercel
.vercel

# testing
coverage/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
.vscode/
*.tsbuildinfo
.env*

# agent skills — skill definitions fetched by local tooling, the symlinks under
# .claude/skills that point into them, and their lockfile. Local development
# aids, not part of the app: nothing in the build or the deploy reads them.
.agents/
.claude/skills/
skills-lock.json
